<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Add New Song</title>
  </head>
  <body>
    <h1>Add A New Song</h1>
    <form action="/songs" method="post">
      <label>Song Title</label>
      <input type="text" name="song[name]" value="">
      <br><br>
      <label>Artist</label>
      <input type="text" name="song[artist]" value="">
      <br><br>
      <h3>Genres:</h3>
      <% Genre.all.each do |genre| %>
        <input id="<%= genre.name %>" type="checkbox" name="genres[]" value="<%= genre.id %>"><%=genre.name%><br><br></input>
      <% end %>
      <input type="submit">
    </form>
  </body>
</html>
